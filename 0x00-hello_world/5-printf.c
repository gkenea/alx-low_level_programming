#include <stdio.h>

/**
 * main - Print the given format using printf function
 *
 * Return: always 0 (Success)
 */

int main(void)
i{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
